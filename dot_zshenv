
case ":${PATH}:" in
  *:"$HOME/.cargo/bin":*) ;;
  *) export PATH="$HOME/.cargo/bin:$PATH" ;;
esac

export GHQ_ROOT="$HOME/ghq"

# direnv の loading/unloading ログを完全に抑制(unload も含む)
export DIRENV_LOG_FORMAT=""
